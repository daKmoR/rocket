# Presets >> Search ||20

Add a search for all your static content.

## Installation

```bash
npm i @d4kmor/search
```

👉 `rocket.config.mjs`

```js
import { rocketSearch } from '@d4kmor/search';

export default {
  presets: [rocketSearch()],
};
```
